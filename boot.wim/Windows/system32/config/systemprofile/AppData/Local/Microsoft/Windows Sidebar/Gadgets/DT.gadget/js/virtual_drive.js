var virtualDrive = function(name, title, path, shortcut, tooltip, disabled) {
	this.name = name;
	this.title = title;
	this.path = path;
	this.shortcut = shortcut;
	this.tooltip = tooltip;
	this.disabled = disabled;
}
